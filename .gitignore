# --- Node / package managers ---
node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/
.eslintcache
*.tsbuildinfo

# --- Expo / React Native ---
.expo/
.expo-shared/*
!.expo-shared/assets.json   # keep asset hashes shared with your team
.metro-cache/
.cache/
dist/
web-build/
tmp/
temp/

# --- Environment files (keep secrets out of git) ---
.env
.env.*
!.env.example

# --- OS cruft ---
.DS_Store
Thumbs.db

# --- Editors/IDEs ---
.vscode/
.idea/
*.swp

# --- Testing ---
coverage/
.nyc_output/

# --- Android (if present after `expo prebuild`) ---
android/.gradle/
android/.idea/
android/local.properties
android/**/build/
android/**/captures/
android/**/release/
# Keep the Gradle wrapper if present
!android/gradle/wrapper/gradle-wrapper.jar

# --- iOS (if present after `expo prebuild`) ---
ios/build/
ios/DerivedData/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
# If using CocoaPods, choose one approach:
# Many teams commit Pods/ for reproducible CI. If you prefer not to, uncomment:
# ios/Pods/
# If using Swift Package Manager:
# ios/.swiftpm/

# --- Fastlane (optional) ---
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

# --- Next.js (only if you add web + Next later) ---
.next/

# --- Optional: platform config secrets (uncomment if you don't commit these) ---
# android/app/google-services.json
# ios/GoogleService-Info.plist

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli